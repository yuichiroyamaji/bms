# Implementation Plan

> Phase 3 of [Spec-Driven Development](../../development-process.md). Defines **THE PLAN**.
> Requires **Gate 2** (design) approved. Get **Gate 3** approval before implementing.

Each task should be small enough for one PR, and cite the requirement & test ids it covers.
Implement **test-first** (red → green → refactor) using `test-cases.md`.
Substantial tasks become GitHub **sub-issues** of the Spec epic; small ones stay as checkboxes here.

- [ ] 1. `<task>` — `(Req 1.1 · TC-1)`
- [ ] 2. `<task>` — `(Req 1.2, 2.1 · TC-2)`
  - [ ] 2a. `<sub-step>`
  - [ ] 2b. `<sub-step>`
- [ ] 3. `<task>` — `(Req 3.1 · TC-3)`
- [ ] 4. Update docs / spec / test-cases if implementation changed the design

## Notes

<!-- Sequencing constraints, dependencies, anything an implementer needs to know. -->
