# Requirements Document

> Phase 1 of [Spec-Driven Development](../../development-process.md). Defines **WHAT & WHY**.
> Get **Gate 1** approval before writing `design.md`.

## Introduction

<!-- 1–3 sentences: what this feature/initiative is and the problem it solves. -->

## Glossary

<!-- Define domain terms used below so requirements are unambiguous. -->
- **Term**: definition

## Requirements

### Requirement 1

**User Story:** As a `<role>`, I want `<capability>`, so that `<benefit>`.

#### Acceptance Criteria

<!-- EARS notation. Use WHEN (event), WHERE (state/precondition), IF (optional). -->
1. WHEN `<trigger>`, THE `<system>` SHALL `<response>`
2. WHERE `<precondition>`, THE `<system>` SHALL `<response>`
3. IF `<condition>`, THEN THE `<system>` SHALL `<response>`

### Requirement 2

**User Story:** As a `<role>`, I want `<capability>`, so that `<benefit>`.

#### Acceptance Criteria

1. WHEN `<trigger>`, THE `<system>` SHALL `<response>`

## Out of scope

<!-- Explicitly list what this spec does NOT cover, to prevent scope creep. -->
-
